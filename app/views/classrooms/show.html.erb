<h1>Classroom Info</h1>
<%= render partial: 'classroom', locals: {classroom: @classroom}%>

<ul>
    <% @classroom.students.each do |s| %>
        <li><%= render partial: 'students/student', locals: {student: s}%></li>
    <% end %>
</ul>
